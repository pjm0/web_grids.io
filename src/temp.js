// Viewport
// API for interacting with canvas
// plot functions (imgX, imgY) => (r, g, b)

// Grid view
// 1 unit between parallel lines
// world coords (imgX, imgY) => (worldX, worldY)
// grid coords (worldX, worldY) => (gridComponent0, gridComponent1, ...)

// grid region (gridComponent0, gridComponent1, ...) => Region
// color at grid coords (gridComponent0, gridComponent1, ...)